version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    environment:
      - DATABASE_URL=postgresql://avian:IeIzpYN*v8cBNxe&@cf.avolut.com:5434/avian_csr?schema=public
    restart: unless-stopped